/*---------------------------------------------------------------------------
 * 建索引计数法
 ---------------------------------------------------------------------------*/
//---------------------------------------------------------------------------
#include <vector>
#include <iostream>
#include <stdlib.h>
#include <assert.h>
//---------------------------------------------------------------------------
struct Item
{
    int key;
    int val;
};
std::vector<std::string> Sort(std::vector<Item> list)
{
    int N = list.size();

    std::vector<int> count(

}
//---------------------------------------------------------------------------
int main(int, char**)
{
    return 0;
}
//---------------------------------------------------------------------------
